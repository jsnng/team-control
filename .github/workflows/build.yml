name: team-control-build

on:
  push:
    branches: ['cpp']

jobs:
  build:
    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: false
    - run: |
        (mkdir build/) -and (cd build/)
        cmake ../
        cmake --build .